extern crate cryptography;

use cryptography::monocycle_permutation::MonocyclePermutation;

fn test(message: Vec<char>) {
    let equal_parts: Vec<(Vec<char>, Vec<usize>)> = Vec::new();

}

fn main() {
    let equal_parts: Vec<(Vec<char>, Vec<usize>)> = Vec::new();
    let mut input = std::io::stdin();
    let mut buffer = String::new();
    input.read_line(&mut buffer).unwrap();
    for i in 0..buffer.len() {
        for j in i + 1..buffer.len() {
            
        }
    }
}
